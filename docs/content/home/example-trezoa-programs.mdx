---
title: Trezoa Programs Example
---

{/* Please keep visible code between 4 to 8 lines of code. */}

```ts
import { getTransferTrzInstruction } from '@trezoa-program/system';
const transferTrz = getTransferTrzInstruction({ source, destination, amount });

import { fetchMint } from '@trezoa-program/token';
const mintAccount = await fetchMint(rpc, mintAddress);
```
